db:
  image: postgres
ubuntu:
  image: ubuntu
web:
  build: fig/services/ubuntu
  command: python ./manage.py runserver 0.0.0.0:8000
  volumes:
    - .:/code
  ports:
    - "8000:8000"
  links:
    - db
    - ubuntu